export const BubleSortAction = async (value) => {
    try{
        let array = value;
      //  let buble = array.sort((i, j) => { return i - j })
        await  value
    }catch(err){
        console.log(err)
    }
  
    // await route.push("/pagetwo")
} 


